<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTest"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CountDownActivity"
            android:exported="true"
            />

        <activity android:name="com.abrantix.roundedvideo.example.MainActivity"
            android:exported="true"
            />

        <activity android:name="com.fuusy.floatwindow.MainActivity"
            android:exported="true"
            />

        <activity android:name="com.fuusy.floatwindow.VideoPlayActivity"
            android:exported="true"
            />

        <service android:name=".floating.FloatingSerivice"
            android:exported="true"
            />

    </application>

</manifest>